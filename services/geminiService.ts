// Servicio desactivado a pedido del usuario.
// Se mantiene la estructura para evitar errores de importación en otros archivos.

export const analyzeReceiptImage = async (base64Image: string) => {
  console.log("Análisis por IA desactivado temporalmente.");
  return null;
};
